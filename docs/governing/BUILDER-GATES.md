> **MIRROR — READ-ONLY (mirrored 2026-07-01).** Canonical source: `/Users/philipp/Documents/Work/Miden-Coding/agentic-template/ai-tasks/circle-integration/07-implementation-readiness/BUILDER-GATES.md`. Do NOT edit this copy; if it diverges from the canonical source, the canonical source wins. Re-sync via `tools/sync-mirrors.sh`.

# BUILDER-GATES (FINALIZED — GOVERNING, 2026-06-11 — MASM-first)

Enforceable gates for every implementation unit. Phrased to be **mechanically checkable**, not matters of taste — agent self-assessment of "is this clean / did I review enough" is not gaugeable. Subjective calls (right abstraction? architecture buckling?) are **reserved for the human**.

**Baseline: Miden v0.15 + devnet (2026-06-10, human-authoritative).** The implementation/validation target is **Miden v0.15** on **devnet** (devnet runs v0.15; **testnet is v0.14 and is NOT the validation network**). protocol pins to released tag **v0.15.3**, which locks the internal deps **miden-assembly/core/core-lib/processor 0.23.3** and **miden-crypto/miden-field 0.25.1** (the historical v0.15.1 locked 0.23.1) — these are the **v0.15 stack's dependencies** (kept), not a stale top-level target. The verified pin matrix + devnet endpoints + precompile re-confirmation are in `V15-DEVNET-BASELINE.md`. The earlier `v0.16` / `protocol@0b662adfb` / `miden-vm@328071990` / `0.24`-retarget / testnet framing is superseded.

**MASM-first premise.** The custom contracts (faucet first) are **hand-written MASM**. Their build/test gates are MASM assembly + MockChain + local-node (the local node = `miden-node bundled start`, also v0.15), **not** `cargo miden build` over a Rust contract crate. The toolchain + conventions are now **resolved** in `MASM-STRUCTURE-RESEARCH-REPORT.md` (audited PASS) — that report is the source of truth. Safe builder path: assemble via `miden-standards`' `CodeBuilder` / `TransactionKernel::assembler()` → `AccountComponentCode` → `AccountComponent::new`; note scripts via `NoteScript::from_library_reference`; test via the `miden-testing` MockChain path (report §3). **The assembler stack is settled under v0.15 (spike-validated, 2026-06-08, Codex PASS; re-confirmed under v0.15 by `V15-DEVNET-BASELINE.md` §3): the v0.15 stack's assembler dependency — `miden-assembly`/`miden-core-lib` 0.23.3, exactly what `protocol v0.15.3` locks (the spike verified **Q1–Q4 + the precompile gate** identical on 0.23.1 and 0.23.3; Q5/Q6 were verified on the 0.23.1-seeded stack and carry per `V15-DEVNET-BASELINE.md` §4) — carries the Keccak/ECDSA precompiles, so the old "0.24 retarget" is MOOT (it was never a v0.15 question; `0.23.3` is simply the v0.15 dep) and the safe `CodeBuilder` path is validated end-to-end through MockChain (`08-masm-grounding-spike/`).** A hand-built raw `Assembler` and precompile-host *execution* wiring remain later, human-gated steps (the spike proved precompile presence + assembly-time linkability, not execution — report §3.1/§5.1, spike §2 Q2). **Note:** both gates were re-run under the v0.15/devnet baseline on 2026-06-10 — Gate (A) = **PASS** (`GATE-A-V15-SPEC-REAUDIT-2026-06-10.md`); Gate (B) = **NOT PASS as-run; its sole blocker (NS-1/NS-2/DC-7) was ADJUDICATED by the human on 2026-06-10** (decisions recorded in the ownership map `§Naming splits` + `HUMAN-FINALIZATION-PACKET.md` §2.A; report: `GATE-B-V15-CONSORTIUM-REAUDIT-2026-06-10.md`). This document is FINALIZED — GOVERNING (2026-06-11, human acceptance of the finalization packet); builders remain BLOCKED until the human gives explicit go/no-go.

**Fresh protocol AI/MASM guidance.** Builder and critic agents must include the protocol-local skills added by `0xMiden/protocol#2927` (merged 2026-06-08, merge commit `5bc1655d51011e3c1eb3830564fc0cd23bb35e0e`) as mandatory checklist material before implementing or reviewing any MASM/Rust unit. These skills are hygiene/review guidance, not runtime API evidence: they must not override frozen Phase 4 source truth, Circle-owned open decisions, pinned API evidence, or the MASM grounding spike. If a PR #2927 skill conflicts with local/pinned evidence, stop and report the conflict.

## G0 — Objective gate runs BEFORE any human review
A unit is not eligible for human review until all of these are green (a green build/test is cheap; human attention is not):
- The unit's **source-backed build/assembly gate** is clean:
  - hand-written MASM contract → **assembles** via the safe `CodeBuilder::compile_component_code` / `TransactionKernel::assembler()` path (report §3.3); a hand-built raw `Assembler` is still a `HUMAN DECISION REQUIRED` later step (it touches the version-sensitive raw-link/host layer) — do not use it before that call is made. Under the v0.15 baseline the safe path resolves the v0.15 stack's `0.23.3` assembler dependency (v0.15.3-locked); the old "0.24 retarget" is moot;
  - Rust harness/tooling/test crate → its `cargo` build/test is clean.
- The unit's **happy-path test passes** (written first — see G4). **For any owned routine with a MASM implementation, "passes" means the MASM is EXECUTED against its golden vectors** (via `TransactionContext::execute_code` / MockChain, report §3.9), proving it produces the expected felts — assemble-clean (the build gate above) is necessary but **not** sufficient. **For a note-script unit, the happy path executes the full create→consume cycle** (`NoteBuilder…script(s).build()` → `build_tx_context(unauthenticated_notes)` → assert `output_notes`/`account_delta`); for the public burn note, exercise the two-block create→consume.
- **MockChain** test green for on-chain behavior (resolved path: `MockChain::builder()` → `add_*_account_from_components` → `build_tx_context` → `execute()` → assert on `account_delta`/`output_notes`, report §3.9); **local-node validation green** where the unit touches notes / RPC / lifecycle (gated, after MockChain, report §3.9). The MASM monorepo home on disk is a human decision — do not assume `project-template/` is the monorepo.
- Duplication scan clean (G1).
- File-size / structure check passes (G3).
- **Each gate pins a concrete command + expected-output substring** (e.g. `cargo test --test happy_path` → `test result: ok. N passed; 0 failed`; the assembly probe → `OK (N export(s))`; the duplication and file-size scans), mirroring the spike's exact-command discipline (`08-masm-grounding-spike/GROUNDING-REPORT.md §3`) so G0 is CI-mechanizable, not prose.

## G-MASM — MASM conventions are source-backed, never invented
- MASM **formatting, file-structure, constant placement, padding, and comment** conventions are **resolved** in report §2 (citing the MASM skills `masm-formatting`/`-file-structure`/`-constants`/`-padding`/`-inline-comments`/`-doc-comments` + live `miden-standards`), including the §2.1 section-ordering and §2.2 shim-header adjudications. Follow §2; install the skills (per repo `CLAUDE.md` / `0xMiden` agent-tools) only if extending beyond what the report cites. **No guessed MASM command/convention may gate a unit; do not fake an assembly pass.**
- Apply the PR #2927 MASM skills as additional mandatory checklists for each MASM unit: `cheap-masm-equivalents`, `checked-arithmetic`, `felt-construction`, `u32-assert-before-u32-ops`, `masm-error-constants`, `masm-explicit-stack-inputs`, `masm-locals-over-globals`, `masm-named-literals`, `masm-rust-constant-parity`, and **`advice-provider-hygiene`**. The builder's completion note must list which of these were checked and any resulting changes or conflicts.
- **`advice-provider-hygiene` is MANDATORY and security-critical for the `attestation_verify` proc and the `feeAmount` advice path** (consortium H1). Every advice/`NoteAttachment`-sourced value — the 17-felt ECDSA signature, the 9-felt candidate pubkey, and the operator `feeAmount` — MUST be validated against a kernel-trusted commitment (the pubkey-commitment `assert_eqw` + the keccak-bound signature, frozen `01-onchain-xusdc-faucet/COMPONENT-SPEC.md:218-221,:318`), use content-addressed advice-map keys, and **ERROR (not default) on missing advice**. A missed advice-commitment / missing-advice check on this path is an unlimited-mint authorization bypass — the worst failure mode in the faucet.
- **RESOLVED by the grounding spike** (`08-masm-grounding-spike/`, status GROUNDED, Codex PASS, 2026-06-08; facts carry to the current v0.15.3-locked 0.23.3 stack — the spike verified **Q1–Q4 + the precompile gate** identical on 0.23.1 and 0.23.3; Q5/Q6 were verified on the 0.23.1-seeded stack and carry per `V15-DEVNET-BASELINE.md` §4; the spike's `0b662adfb` pin is `git describe` `v0.15.0-21`, on the v0.15 line) — running-code evidence: core-lib `use` root = **`miden::core::`** (bare `core::` fails); `@note_script`/`@locals(N)` are lowercase line-above attrs with `loc_storew_le`/`loc_loadw_le` access; **`pub proc` alone exports** a component proc (no attribute); `word("ns::label")` + `[0..2]` yields the slot id, cross-checked against Rust `StorageSlotName::new(same label)`; minimal bind = `CodeBuilder::new().compile_component_code` → `StorageSlot::with_value` → `AccountComponentMetadata::new` → `AccountComponent::new`; **no `[build-dependencies]`** (runtime-compile path; custom component into a script via `with_dynamically_linked_library(&code)`); the **Keccak/ECDSA precompiles ARE present & linkable on the v0.15 stack's assembler dependency (`miden-core-lib` — confirmed on BOTH 0.23.1 and the v0.15.3-locked 0.23.3)** — re-confirmed in `V15-DEVNET-BASELINE.md` §3.
- **Genuinely still PENDING (later units, not the spike):** the **map/schema storage** path (`StorageSchema`/map slots) for registries/allowlists; precompile **execution** with registered `Host` handlers; the real **kernel proc stack contracts** (`faucet::mint`/`burn`/`create_fungible_asset`, `output_note::*`, `active_note::*`, `asset::*`); **local-node** validation against the **v0.15 devnet** node (`miden-node bundled start`). Build hygiene: **seed `Cargo.lock` to the v0.15.3-locked dep (`miden-core-lib 0.23.3`)** — the spike verified **Q1–Q4 + the precompile gate** identical on 0.23.1 and 0.23.3; Q5/Q6 were verified on the 0.23.1-seeded stack and carry per `V15-DEVNET-BASELINE.md` §4; precompiles present on both (spike §0 caveat; `V15-DEVNET-BASELINE.md` §3).
- **`uint256→AssetAmount` reducer (DC-5, external Circle amount) — pinned obligations (consortium N11):** the Rust mirror MUST use **checked/overflowing arithmetic that surfaces overflow** on the scale / floor-div / cap path (no bare `*`/`-`/`+` on external values — `checked-arithmetic`); the MASM impl MUST `u32assert`/`u32assert2` the externally-supplied uint256 limbs **before** any `u32*` op (`u32-assert-before-u32-ops`); the golden vectors MUST include a cap-boundary case AND a limb-overflow case.
- **The grounding spike is throwaway PROOF code, NOT a style template (consortium N9/N10).** The `08-masm-grounding-spike/` `.masm` files and their inline `assert.err="..."` strings intentionally skip §2/#2927 MASM conventions (no `#!` doc blocks, no named `ERR_*` constants, magic-literal words). Real xUSDC MASM units MUST follow §2 + this G-MASM list (doc blocks, named `ERR_*` per `masm-error-constants`, named literals per `masm-named-literals`) — do NOT cargo-cult the spike's file shape or inline error strings.

## G1 — Single ownership / no duplication (per language + cross-language conformance)
- Each owned routine in `CANONICAL-OWNERSHIP-MAP.md`: **≤ 1 MASM implementation** and **≤ 1 Rust implementation**, both conforming to the canonical byte/felt contract, **cross-checked by ONE canonical golden-vector artifact owned by shared-encoding(04)** (loaded by reference on both sides — see the map's Anti-duplication rule; a duplicate vector table also fails this scan).
- A second within-language definition of an owned thing — **even byte-identical** — fails (this is the V2-05 seam: the relayer's off-chain mirror must conform to and be pinned to the 04 owner, not re-derived).
- Mechanical check: no duplicate definition of an owned routine outside its single per-language home (e.g., a second MASM `parse_deposit_intent` proc, or a second Rust DepositIntent decoder). Wrappers that merely re-expose an owned routine also fail.
- **Cross-language constant parity (`masm-rust-constant-parity`, #2927, consortium N1):** the DC-1 layout offsets / packed magic `0x5a2e0acd` / DC-5 cap+scale are MASM↔Rust constant pairs — **prefer `build.rs` codegen (one source of truth, report §3.7)** or, where hand-duplicated, change both sides in one unit + cross-check via the canonical vectors. A one-sided constant edit fails this gate (see the map's Anti-duplication rule).

## G2 — Ownership map is the source of truth for boundaries
- No new shared concept / module boundary / cross-component interface without **first** adding it to `CANONICAL-OWNERSHIP-MAP.md` (human re-approves). No silent new shared shapes.

## G3 — Module/file size + structure
- Default file ceiling **~500–700 lines** for Rust. Research found **no mandated MASM file-size rule** in source (report §5.10–§5.11); split `.masm` logically per the two-root layout (one routine-family per module under `asm/standards/{xreserve,encoding,notes}/`) rather than by a guessed line count. Past a Rust ceiling → stop and split before continuing.
- **D-1A module-realization rule (ratified 2026-06-12 — overrides any per-file sketch):** under miden-assembly 0.23.3, procs whose canonical path is FLAT `xreserve::encoding::<name>` MUST live in the directory root `encoding/mod.masm` (per-file `.masm` ⇒ nested module paths; wrapper aliases banned per G1/NS-1). See the ownership map §MASM module realization + `P5-04-SHARED-ENCODING-ACCEPTANCE-RECORD.md`.
- Tests live in their **own module/file**, not inline with implementation.
- Final layout target: **MASM monorepo, one folder per component** — shape **resolved** (two-root `asm/standards` + `asm/account_components` tree; see `CANONICAL-OWNERSHIP-MAP` §Resolved layout). Permanent on-disk home = human decision.

## G4 — Test discipline (happy path first)
- The **happy-path test is written first**, before any negative/edge/malformed case, and before the unit is considered done.
- Negative / malformed-input / replay / boundary cases follow — and do **not** substitute for happy-path coverage.
- Repeated setup is factored into **shared fixtures** (no copy-pasted setup).
- Encoding routines carry **deterministic golden vectors** (input → expected bytes/felts), including documented edges (cap boundary, limb overflow), in **ONE canonical artifact owned by shared-encoding(04)**. BOTH the MASM-side test and the Rust-side test **load those vectors by reference** (not hand-copied tables) and both **execute** against them — two executed runs over one vector set, not one executed + one assembled. A duplicate vector table outside the 04 home fails the G1 duplication scan, same as a duplicate routine (closes the V2-05 seam at the test layer).
- **Negative/reject tests assert the SPECIFIC error**, not `is_err()` (`assert-specific-error-in-tests` + `masm-error-constants`, consortium N6): Rust via `assert_matches!` on the error variant/fields; MASM via asserting the trapping code equals the unit's named `ERR_*` constant — keyed to the frozen `R-MINT-*`/`R-BURN-*`/`R-ADMIN-*` ids (`01-onchain-xusdc-faucet/TEST-AND-VERIFICATION-HARNESS.md` §4 — ALL reject rows incl. §4.A mint-deny, §4.C–§4.O, §4.K admin, and the burn/note rejects — is the binding catalog; the §4.C–§4.O span is illustrative, not the boundary).
- **Parametrize repeated reject families** (`parametrize-related-tests`, consortium N7): when ≥2 tests share a body and differ only by (mutation, expected `ERR_*`), express them as ONE `#[rstest]` with one `#[case::<name>]` per case — the assertion lives once and a missing case is visible (e.g. the §4.C 8-field DepositIntent reject table).

## G-RUST — off-chain Rust harness/tooling hygiene (#2927, consortium N4/N8)
The off-chain harness/tooling crates (relayer, listener, monitor, MASM loaders, test fixtures) are gated by the PR #2927 Rust skills as a mandatory per-unit checklist; the builder's completion note lists which were checked:
- API/design: `domain-newtypes-over-primitives`, `private-fields-with-accessors`, `non-exhaustive-public-types`, `validate-in-constructor`, `conversion-method-naming`, `use-bon-builder`, `intra-doc-links`, `workspace-shared-dependencies`. (Conform to / wrap the frozen 04 Rust signatures — do NOT edit the frozen spec; new harness types may newtype-wrap the raw `&[u8]`/`Word`/tuple boundaries.)
- **`return-error-not-panic` + `preserve-error-source` + `lowercase-error-messages` — MANDATORY on any code reachable from Circle/RPC/note input** (the relayer DepositIntent pre-validate, the listener JSON / burn-evidence decode, `AccountId↔bytes32`): surface failures as a typed `Result` — **no `unwrap`/`expect`/`panic!`/`unwrap_or_default` on external-derived data**; a missing required input is an error (never default-fabricated); preserve the source chain (`#[source]`/`#[from]`, no `.to_string()` flattening); lowercase, un-punctuated messages. A relayer/listener that panics or default-fabricates on a malformed Circle payload is a DoS and can mask a V2-05 divergence from the 04 owner.

## G5 — Reality grounds the spec
- **`§Naming splits` STOP rule — NS-1/NS-2 ADJUDICATED (human, 2026-06-10; STOP condition SATISFIED for them).** Decisions binding on every builder: **NS-1** → canonical MASM proc `xreserve::encoding::bytes32_to_key` (the `01:324` `bytes32_to_storage_map_key` is a superseded 01 consumer-side symbol — NOT a second proc, NOT a wrapper alias); **NS-2** → the shared DepositIntent parser is **04-owned `xreserve::encoding::parse_deposit_intent`** (01 may own distinct mint-specific validation/assertion logic but must NOT duplicate or re-own the shared parser). **DC-7** also ratified: `encoding/burn_items.masm` = 04-owned codec home (faucet note behavior 01-owned; listener decoding conforms to the 04 codec). [SUPERSEDED 2026-06-30 → P5-04 codec: DC-7 shipped as RUST (`encoding/burn_note.rs`), NOT a .masm file — there is no burn_items.masm] The STOP rule remains in force for any FUTURE unadjudicated split in the map's `§Naming splits`: the builder MUST STOP at the affected routine and return to the human — NO silent pick, NO dual-named wrapper, NO local alias.
- **Code wins over spec.** If running MASM/Rust cannot match the spec (a felt count, a Word layout, an RPC semantic that real `protocol`/`miden-vm`/node contradicts), the builder **STOPS and reports the contradiction** — it does not force code to fit a wrong spec, and does not silently diverge. The human decides whether to correct the frozen archive.
- **No hand-wavy Miden fake in a gating test.** A speed-only adapter fake is allowed only if labelled NON-GATING and paired with a real MockChain / local-node / source-backed check.
- **No local-node skip** after MockChain passes (where the unit touches notes/RPC/lifecycle).

## G6 — Open decisions stay open
- No Circle-owned `DEV-*`/`Q-*` may be marked approved/accepted/resolved anywhere. Contested Circle paths are built behind clearly-flagged assumptions or not at all until Circle answers.

## G7 — Concurrency
- Sequential builders at first; **no two agents edit the same module family in parallel**; no broad parallel implementation.

## G8 — Human merge gate (non-delegable)
- After G0 is green and the diagnostic critics have reported, **the human reads the diff and approves** before merge. A critic agent's "looks good" is **never** the gate. Agents draft and diagnose; the human approves.

*FINALIZED — GOVERNING (2026-06-11, human acceptance of `HUMAN-FINALIZATION-PACKET.md`). MASM research + grounding spike + consortium are done; their results (the v0.15 stack's assembler dependency — `0.23.3` at v0.15.3, verified on both 0.23.1 and 0.23.3 — is sufficient with precompiles present; execute-not-just-assemble; single canonical vectors; note create→consume; pinned commands) are folded in above and retargeted to the Miden v0.15 + devnet baseline (`V15-DEVNET-BASELINE.md`). The 2026-06-10 v0.15/devnet gate re-runs are complete — Gate (A) PASS; Gate (B)'s blocker (NS-1/NS-2/DC-7) adjudicated by the human the same day (`READINESS-STATUS.md`); finalized 2026-06-11; remaining: the human's explicit implementation go/no-go ONLY. Prefer mechanizing any prose gate into a CI check.*
